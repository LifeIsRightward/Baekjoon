for (int j = 0; i < n; j++) {
        //     cin >> name >> kind;
        //     if (f1.find(kind) != f1.end()) {
        //         // 같은 종류의 의상이 map에 있으면, ++ 시켜줌
        //         f1[kind]++;
        //     } else {
        //         // 같은 종류의 의상이 map에 없으면, 1로 초기화
        //         f1[kind] = 1;
        //     }
        // }