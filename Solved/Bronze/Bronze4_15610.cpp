#include <bits/stdc++.h>

using namespace std;

int main() {

    double a = 0;

    cin >> a;

    double b = sqrt(a);

    cout << fixed;
    cout.precision(7);
    cout << 4 * b << "\n";

    return 0;
}