#include <bits/stdc++.h>

using namespace std;

int main() {
  // FAST IO
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  long long n, m = 0;

  cin >> n >> m;

  cout << (n * m) / 2 << "\n";
  return 0;
}